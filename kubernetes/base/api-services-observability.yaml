apiVersion: v1
kind: Service
metadata:
  name: health-monitor-service
spec:
  selector:
    app: health-monitor-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-monitor-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: health-monitor-service
  template:
    metadata:
      labels:
        app: health-monitor-service
    spec:
      initContainers:
        - name: wait-for-health-monitor-db
          image: asset_monitoring_system-health-monitor-service:latest
          imagePullPolicy: IfNotPresent
          command: ["sh", "-lc", "until php -r '$s=@fsockopen(\"health-monitor-db\",5432); if ($s) { fclose($s); exit(0);} exit(1);'; do echo waiting for health-monitor-db; sleep 2; done"]
        - name: wait-for-rabbitmq
          image: asset_monitoring_system-health-monitor-service:latest
          imagePullPolicy: IfNotPresent
          command: ["sh", "-lc", "until php -r '$s=@fsockopen(\"rabbitmq\",5672); if ($s) { fclose($s); exit(0);} exit(1);'; do echo waiting for rabbitmq; sleep 2; done"]
      containers:
        - name: health-monitor-service
          image: asset_monitoring_system-health-monitor-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - {name: APP_ENV, value: local}
            - {name: APP_DEBUG, value: "true"}
            - {name: APP_URL, value: http://api.asset-monitoring-system.local/api/v1/health}
            - {name: DB_CONNECTION, value: pgsql}
            - {name: DB_HOST, value: health-monitor-db}
            - {name: DB_PORT, value: "5432"}
            - {name: DB_DATABASE, value: health_monitor_db}
            - name: DB_USERNAME
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: db-username}}
            - name: DB_PASSWORD
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: db-password}}
            - {name: QUEUE_CONNECTION, value: sync}
            - {name: CACHE_STORE, value: file}
            - {name: SESSION_DRIVER, value: file}
            - {name: LOG_CHANNEL, value: stack}
            - {name: DEVICE_INACTIVE_AFTER_MINUTES, value: "10"}
            - {name: RABBITMQ_HOST, value: rabbitmq}
            - {name: RABBITMQ_PORT, value: "5672"}
            - name: RABBITMQ_USER
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-user}}
            - name: RABBITMQ_PASSWORD
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-password}}
            - name: RABBITMQ_VHOST
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-vhost}}
            - name: RABBITMQ_EXCHANGE
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-exchange}}
            - {name: RABBITMQ_QUEUE_PREFIX, value: health}
            - {name: TELESCOPE_ENABLED, value: "true"}
            - {name: TELESCOPE_PATH, value: health/telescope}
            - {name: TELESCOPE_SERVICE_NAME, value: health-monitor-service}
          readinessProbe:
            httpGet: {path: /up, port: 8000}
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /up, port: 8000}
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
spec:
  selector:
    app: audit-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      initContainers:
        - name: wait-for-audit-db
          image: asset_monitoring_system-audit-service:latest
          imagePullPolicy: IfNotPresent
          command: ["sh", "-lc", "until php -r '$s=@fsockopen(\"audit-db\",5432); if ($s) { fclose($s); exit(0);} exit(1);'; do echo waiting for audit-db; sleep 2; done"]
        - name: wait-for-rabbitmq
          image: asset_monitoring_system-audit-service:latest
          imagePullPolicy: IfNotPresent
          command: ["sh", "-lc", "until php -r '$s=@fsockopen(\"rabbitmq\",5672); if ($s) { fclose($s); exit(0);} exit(1);'; do echo waiting for rabbitmq; sleep 2; done"]
      containers:
        - name: audit-service
          image: asset_monitoring_system-audit-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - {name: APP_ENV, value: local}
            - {name: APP_DEBUG, value: "true"}
            - {name: APP_URL, value: http://api.asset-monitoring-system.local/api/v1/audit}
            - {name: DB_CONNECTION, value: pgsql}
            - {name: DB_HOST, value: audit-db}
            - {name: DB_PORT, value: "5432"}
            - {name: DB_DATABASE, value: audit_db}
            - name: DB_USERNAME
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: db-username}}
            - name: DB_PASSWORD
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: db-password}}
            - {name: QUEUE_CONNECTION, value: sync}
            - {name: CACHE_STORE, value: file}
            - {name: SESSION_DRIVER, value: file}
            - {name: LOG_CHANNEL, value: stack}
            - {name: RABBITMQ_HOST, value: rabbitmq}
            - {name: RABBITMQ_PORT, value: "5672"}
            - name: RABBITMQ_USER
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-user}}
            - name: RABBITMQ_PASSWORD
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-password}}
            - name: RABBITMQ_VHOST
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-vhost}}
            - name: RABBITMQ_EXCHANGE
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-exchange}}
            - {name: RABBITMQ_QUEUE_PREFIX, value: audit}
            - {name: TELESCOPE_ENABLED, value: "true"}
            - {name: TELESCOPE_PATH, value: audit/telescope}
            - {name: TELESCOPE_SERVICE_NAME, value: audit-service}
          readinessProbe:
            httpGet: {path: /up, port: 8000}
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /up, port: 8000}
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      initContainers:
        - name: wait-for-notification-db
          image: asset_monitoring_system-notification-service:latest
          imagePullPolicy: IfNotPresent
          command: ["sh", "-lc", "until php -r '$s=@fsockopen(\"notification-db\",5432); if ($s) { fclose($s); exit(0);} exit(1);'; do echo waiting for notification-db; sleep 2; done"]
        - name: wait-for-rabbitmq
          image: asset_monitoring_system-notification-service:latest
          imagePullPolicy: IfNotPresent
          command: ["sh", "-lc", "until php -r '$s=@fsockopen(\"rabbitmq\",5672); if ($s) { fclose($s); exit(0);} exit(1);'; do echo waiting for rabbitmq; sleep 2; done"]
        - name: wait-for-mailpit
          image: asset_monitoring_system-notification-service:latest
          imagePullPolicy: IfNotPresent
          command: ["sh", "-lc", "until php -r '$s=@fsockopen(\"mailpit\",1025); if ($s) { fclose($s); exit(0);} exit(1);'; do echo waiting for mailpit; sleep 2; done"]
      containers:
        - name: notification-service
          image: asset_monitoring_system-notification-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - {name: APP_ENV, value: local}
            - {name: APP_DEBUG, value: "true"}
            - {name: APP_URL, value: http://api.asset-monitoring-system.local/api/v1/notifications}
            - {name: DB_CONNECTION, value: pgsql}
            - {name: DB_HOST, value: notification-db}
            - {name: DB_PORT, value: "5432"}
            - {name: DB_DATABASE, value: notification_db}
            - name: DB_USERNAME
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: db-username}}
            - name: DB_PASSWORD
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: db-password}}
            - {name: QUEUE_CONNECTION, value: sync}
            - {name: CACHE_STORE, value: file}
            - {name: SESSION_DRIVER, value: file}
            - {name: LOG_CHANNEL, value: stack}
            - {name: MAIL_MAILER, value: smtp}
            - {name: MAIL_HOST, value: mailpit}
            - {name: MAIL_PORT, value: "1025"}
            - name: MAIL_FROM_ADDRESS
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: mail-from-address}}
            - {name: MAIL_FROM_NAME, value: AssetMonitoringSystem}
            - name: SLACK_WEBHOOK_URL
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: slack-webhook-url}}
            - {name: RABBITMQ_HOST, value: rabbitmq}
            - {name: RABBITMQ_PORT, value: "5672"}
            - name: RABBITMQ_USER
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-user}}
            - name: RABBITMQ_PASSWORD
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-password}}
            - name: RABBITMQ_VHOST
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-vhost}}
            - name: RABBITMQ_EXCHANGE
              valueFrom: {secretKeyRef: {name: asset-monitoring-system-secrets, key: rabbitmq-exchange}}
            - {name: RABBITMQ_QUEUE_PREFIX, value: notification}
            - {name: TELESCOPE_ENABLED, value: "true"}
            - {name: TELESCOPE_PATH, value: notifications/telescope}
            - {name: TELESCOPE_SERVICE_NAME, value: notification-service}
          readinessProbe:
            httpGet: {path: /up, port: 8000}
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /up, port: 8000}
            initialDelaySeconds: 30
            periodSeconds: 20
